.seven.columns
  .panel
    %h4{style: "text-align: center"} Товары в Вашей Корзине
    .panel
      %ul
        - @products.each do |p|
          %li= p.name
          %li= "Количество: #{session[:products][p.id]}"
    .div{style: "text-align: center"}
      = link_to 'Оформить Покупку', new_order_path, class: 'small button'
      = link_to 'Вернуться к Просмотру Продуктов', root_path, class: "small button"